import { Empty, money } from './ChartCommon'
export default function TopProductsTable({ data, loading }) { if (!data.length) return <Empty loading={loading} />; return <div className="table-wrap"><table><thead><tr><th>Rank</th><th>Product</th><th>Category</th><th>Units sold</th><th>Revenue</th></tr></thead><tbody>{data.map((row, i) => <tr key={row.product}><td><span className="rank">{i + 1}</span></td><td className="product">{row.product}</td><td><span className="tag">{row.category}</span></td><td>{row.units_sold}</td><td className="revenue">{money(row.total_revenue)}</td></tr>)}</tbody></table></div> }
