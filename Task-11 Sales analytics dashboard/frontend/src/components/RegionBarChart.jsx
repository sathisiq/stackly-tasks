import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { Empty, money } from './ChartCommon'
export default function RegionBarChart({ data, loading }) { if (!data.length) return <Empty loading={loading} />; return <div className="chart"><ResponsiveContainer width="100%" height="100%"><BarChart layout="vertical" data={data} margin={{ top: 8, right: 14, left: 10, bottom: 0 }}><CartesianGrid horizontal={false} stroke="#edf0f6" /><XAxis type="number" tickFormatter={money} tick={{ fill: '#81879a', fontSize: 11 }} tickLine={false} axisLine={false} /><YAxis type="category" dataKey="region" width={48} tick={{ fill: '#596076', fontSize: 12 }} tickLine={false} axisLine={false} /><Tooltip formatter={(v) => [money(v), 'Revenue']} /><Bar dataKey="revenue" fill="#f4b56b" radius={[0, 6, 6, 0]} barSize={22} /></BarChart></ResponsiveContainer></div> }
