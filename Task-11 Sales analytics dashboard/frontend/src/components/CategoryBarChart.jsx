import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { Empty, money } from './ChartCommon'
export default function CategoryBarChart({ data, loading }) { if (!data.length) return <Empty loading={loading} />; return <div className="chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} margin={{ top: 8, right: 5, left: 0, bottom: 0 }}><CartesianGrid vertical={false} stroke="#edf0f6" /><XAxis dataKey="category" tick={{ fill: '#81879a', fontSize: 11 }} tickLine={false} axisLine={false} /><YAxis tickFormatter={money} tick={{ fill: '#81879a', fontSize: 11 }} tickLine={false} axisLine={false} /><Tooltip formatter={(v) => [money(v), 'Revenue']} /><Bar dataKey="revenue" fill="#75bda9" radius={[6, 6, 0, 0]} /></BarChart></ResponsiveContainer></div> }
